/**
 * 統一設定檔 — 所有常數集中管理
 * ============================================
 * 修改此檔案中的數值後，對應功能的行為會自動改變。
 * 命名規則：大寫底線（UPPER_SNAKE_CASE）
 */

/** 網格大小（世界座標單位） */
export const GRID_SIZE = 20

/** 捕捉容許距離（螢幕像素） */
export const SNAP_THRESHOLD = 10

/** 偏移（Offset）預設距離（世界座標單位） */
export const OFFSET_DISTANCE = 15

/** 標註箭頭大小（螢幕像素倍率 × zoom） */
export const ARROW_SIZE = 5

/** 複製（Copy）操作的 X 軸偏移（世界座標單位） */
export const COPY_DX = 10

/** 複製（Copy）操作的 Y 軸偏移（世界座標單位） */
export const COPY_DY = 10

/** 捕捉指示器內圓半徑（螢幕像素倍率 × zoom） */
export const SNAP_INDICATOR_RADIUS_INNER = 5

/** 捕捉指示器外圓半徑（螢幕像素倍率 × zoom） */
export const SNAP_INDICATOR_RADIUS_OUTER = 8

/** 線性標註偏移預設值（螢幕像素） */
export const DIM_OFFSET_DEFAULT = 15

/** 線性標註偏移最小值（螢幕像素） */
export const DIM_OFFSET_MIN = 10

/** 線性標註偏移最大值（螢幕像素） */
export const DIM_OFFSET_MAX = 100

/** 角度標註弧半徑預設值（世界座標單位） */
export const DIM_ARC_RADIUS_DEFAULT = 20
